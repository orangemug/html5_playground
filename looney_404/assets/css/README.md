The css is built using sass install it from [here](http://sass-lang.com). To convert the sass to css simply run

    sass app.sass:app.css

Or to start a watch

    sass --watch app.sass:app.css
    
